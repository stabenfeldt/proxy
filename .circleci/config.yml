version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout

     # install dependencies
     - run: apk add --no-cache make

     # build and run the application image
     - run: make test

